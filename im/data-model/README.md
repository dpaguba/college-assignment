# The data model

The furniture chain of tutorial five, from the diagram to the tables.

| Module | Topic |
|---|---|
| [furniture-schema](furniture-schema/) | entity types, relations, cardinalities |
| [relational-mapping](relational-mapping/) | the mapping rules and where they disagree with the tutorial |
| [referential-integrity](referential-integrity/) | order of creation, orphans, on-delete |

The schema is worth the attention because it contains the two cases that make
mapping interesting: a relation of an entity type with itself, and a relation
between three entity types.
