# XML

Semi-structured data next to the relational model: the ordered, possibly
irregular tree, the path language that queries it, and what happens when it
is forced into tables.

The XPath module implements positional predicates the way the standard
defines them, per parent rather than per document, and refuses predicates it
does not implement instead of ignoring them.
