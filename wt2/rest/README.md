# RESTful web services

The methods and what they promise, the status codes and the pairs that get
confused, addresses that name things, the maturity model, and the negotiation
of the format.

The idempotence table is measured on a state model over 200 random cases
rather than quoted, and the measurement turns up the case the table leaves
open: whether a PATCH is idempotent depends on its body.
