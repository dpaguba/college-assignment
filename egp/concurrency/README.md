# Concurrency

Lecture 3, minus the transaction material that `is` already covers.

| Module | Topic |
|---|---|
| [interleavings](interleavings/) | how many runs a set of processes has |
| [classical-problems](classical-problems/) | the four patterns and their failure modes |

Both modules exist to make one point concrete: the number of possible runs
grows faster than any test suite, which is why this field computes rather
than experiments.
