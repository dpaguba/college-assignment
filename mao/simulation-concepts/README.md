# Simulation concepts

| Topic | |
|---|---|
| [event-scheduling](event-scheduling/) | the engine every simulation is built on |
| [queueing-models](queueing-models/) | the model with a known answer |
| [model-validation](model-validation/) | three questions that are not the same |
| [simulation-limits](simulation-limits/) | what the ninth sheet asks for |

Chapters two, seven and eight. The block's usable result is the single server
queue: the simulator run for 40 000 customers gives 1.974 where the formula
gives 2.0, and every other module in the subject is checked against something
in the same spirit.

The queue also carries the warning. At 99 percent load it holds 99 customers
against 1 at half load, so a capacity study that reports averages without the
load is reporting almost nothing.
