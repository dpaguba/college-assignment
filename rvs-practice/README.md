# Rechnernetze, practical work

| Folder | What it is |
|---|---|
| [client-server-app](client-server-app/) | a TCP client and server in Java |

The written homework of the course is analytical rather than programmed:
subnetting, distance vector tables, protocol calculations. Those are worked
through as a library in [rvs](../rvs/), where the two published solutions,
the subnet allocation and the distance vector convergence, are reproduced.
