# Learning

| Topic | |
|---|---|
| [entropy](entropy/) | measuring information, and measuring a split |
| [decision-trees](decision-trees/) | greedy splitting, and what it overfits |
| [support-vector-machines](support-vector-machines/) | the widest band |
| [neural-networks](neural-networks/) | what a layer can draw |
| [clustering](clustering/) | grouping without labels |
| [bias-variance](bias-variance/) | why the best model is in the middle |

Lectures six and eight to ten. The eighth sheet's entropy question is
reproduced exactly at 2.7516 bits, and the rest of the block is about the
same trade seen from several sides.

A decision tree of unlimited depth reaches 99 percent on its training data
and 81 on new data. A degree nine polynomial fits twelve points with no bias
and a variance of 29 145. A single neuron cannot learn the exclusive or and a
hidden layer can, but only because the activation is not linear. In every
case the flexibility that fits the data is the flexibility that fits the
noise.
