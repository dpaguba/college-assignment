# Data mining

Three methods and the limit of each: a decision tree that cannot separate two
people whose attributes are identical, a support vector machine whose
solution depends only on the points at the margin, and a neuron that cannot
compute equivalence because two convex hulls overlap.

The hull-distance computation is shared between the last two: separability by
a hyperplane and separability by a neuron are the same question.
