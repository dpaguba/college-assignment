# Visualisation pipeline

The way from data to an image: which colour model to write the values in,
the transformations that place them, the clipping and rasterisation that turn
geometry into pixels, and the question that comes before all of them, which
operations the numbers admit at all.
