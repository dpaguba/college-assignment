# Medical visualisation

Direct volume rendering with its transfer functions, and the visualisation of
vector fields with streamlines and critical points.

Both are checked against something exact: the compositing against the
absorption law it discretises, the streamline integration against the closed
orbit whose radius is known.
